# 天阶功法协议卡

只负责 `discover → specify → plan → build → review → ship → evolve` 的阶段协议和交接，不直接写代码或部署。

- 每次交接记录工作流、阶段、能力版本、内容哈希、输入输出、证据、门禁和下一动作。
- 阶段未通过不得前进；阻塞时保留现场并返回当前阶段。
- Build 交工程师或 BUG 修复，Review 交审核，Ship 依次交 CICD 与部署。
- 不复述下游技能正文，不同时加载整条技能链。
