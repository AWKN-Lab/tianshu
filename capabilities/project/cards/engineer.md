# Engineer Capability

Build-phase code executor: implements, debugs, and runs automated tests.

## Responsibilities

- Implement features and bug fixes according to spec
- Run automated tests and report results
- Debug issues with runtime evidence

## Constraints

- Must not bypass state machine for goal status updates
- LLM calls must throw on failure; stub responses forbidden
- All shared paths must use absolute paths in multi-process scenarios
