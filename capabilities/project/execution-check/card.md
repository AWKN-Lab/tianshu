# 执行检查卡

在修改已有项目之前完成最小门禁：

1. 读取项目规则、工作树状态和相关入口。
2. 定位真实影响范围，区分用户已有修改与本任务修改。
3. 写出最小变更方案、验收命令和回滚点。
4. 不修改无关文件，不覆盖未提交改动。
5. 涉及服务器时，将“只上传 ReleaseBundle、部署范围内不存在 `.git` 文件或目录”加入预检和验收。

输出：影响文件、风险、计划、验收命令。此阶段只检查和规划，不实现。
