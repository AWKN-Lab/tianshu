# 独立审核卡

只负责 Review 门禁，不替代实现者。

- 检查正确性、回归风险、安全性、测试充分性和发布边界。
- 发布门必须验证产物不含名为 `.git` 的路径组件，服务器方案不创建 Git 工作树，也不执行 `git clone/fetch/pull/checkout/reset`。
- 结论必须为 `PASS` 或 `FAIL`，并附文件位置和可复验证据。
- 审核者不得修改代码后直接给自己 PASS；整改应交回工程师，再重新审核。
- 不执行 CICD 或生产部署。
