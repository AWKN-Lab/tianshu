# 天火 deep-mode

定位: 不考虑 token 时的深度任务模式。默认不启用。

## 触发

- 用户显式要求: 深度、完整、全面、不考虑 tokens。
- 任务 3+ 步、长期、多轮研究、跨文件/跨模块。
- 需要阶段验收、文件化计划或持续恢复上下文。

## 读取

1. P0: `agent.prompt`、`SOUL.md`、`BOUNDARY.md`、`MEMORY.md#P0-轻量入口`。
2. P1: `SOP.md`、`CAPABILITY.md`。
3. 工程: 按 `archive/AWKN-PROGRAMMER.md` 本地索引读取。
4. 意图: 按 `archive/ENTROCAMP.md` 读取。
5. 工作记忆: 按 `archive/PLAN-SKILL.md` 使用根目录 `task_plan.md`、`findings.md`、`progress.md`。
6. 进化: 按 `archive/AGENT-OPS.md` 读取自主决策和自我改进规则。
7. 专项工程流水线: 需要 QA/评审/安全/发布/性能时读 `archive/GSTACK.md`。

## 文件化计划规则

- 复杂任务先形成 `task_plan.md`，或明确说明为什么本轮不需要。
- 每 2 次重要探索后，把发现落到 `findings.md` 或交付物摘要中。
- 阶段变化、错误、验证结果写入 `progress.md`。
- 外部资料不要写入 `task_plan.md`，只写摘要到 `findings.md`。

## 输出要求

输出时仍保持用户可读，不倾倒内部全部上下文。必须给:
- 当前目标
- 本轮边界
- 验证方式
- 风险/回滚
- 写回决策
