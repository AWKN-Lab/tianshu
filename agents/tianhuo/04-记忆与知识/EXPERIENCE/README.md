# 经验存储模块

**版本**: v7.0
**创建日期**: 2026-04-22
**目标**: 捕获、存储、结构化天火智能体的执行经验
**位置**: `04-记忆与知识/EXPERIENCE/`

---

## 一、经验捕获架构

### 1.1 经验分类体系

| 类别 | 代码 | 描述 | 存储位置 |
|------|------|------|----------|
| **FIX** (修复经验) | FIX | 从错误/失败中学习的教训 | `fixes/` |
| **DERIVED** (推导经验) | DRV | 从成功中提炼的方法论 | `derived/` |
| **CAPTURED** (捕获经验) | CAP | 突发灵感/观察/创新 | `captured/` |
| **SCAR** (治理伤疤) | SCAR | 系统性治理失误、反复触发的高风险模式 | `scars/` |

### 1.2 经验条目结构

```yaml
经验ID: EXP-{类别}-{YYYYMMDD}-{序号}
标题: {简短描述}
类别: {FIX/DRV/CAP}
来源任务: {task_id}
时间戳: {ISO8601}
作者: 天火

## 经验内容
### 问题/场景
{描述问题或触发场景}

### 解决方案/发现
{描述解决方案或关键发现}

### 关键洞察
{提炼的核心洞见}

### 可复用模式
{抽象化的可复用模式}

### 实施建议
{如何应用到未来任务}

### 证据链
- {证据1}
- {证据2}

### 关联记忆
- MEMORY.md 锚点: {引用}
- 相关技能: {技能名}

版本: 1.0
状态: DRAFT
```

---

## 二、索引管理

### 2.1 经验索引文件

| 文件 | 作用 |
|------|------|
| `INDEX.md` | 全量经验索引（按时间/类别/标签） |
| `TAGS.md` | 标签索引（快速检索） |
| `PATTERNS.md` | 模式索引（跨条目关联） |

### 2.2 索引更新规则

- 新经验写入后 **立即更新** INDEX.md
- 每 7 天执行一次 PATTERNS.md 重构
- 标签在写入时 **实时添加**

---

## 三、版本控制

### 3.1 版本号规则

```
主版本.次版本
- 主版本: 架构变更
- 次版本: 内容更新
```

### 3.2 版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.0 | 2026-04-22 | 初始版本 |

---

## 四、验证清单

经验写入后必须验证：

- [ ] 经验ID唯一
- [ ] 必需字段完整
- [ ] 证据链有效
- [ ] INDEX.md已更新
- [ ] 关联记忆已引用

---

## 五、写回规则

默认启动只读 `MEMORY.md#P0-轻量入口`，不读本目录。

| 任务结果 | 写回位置 | 要求 |
|----------|----------|------|
| 成功且可复用 | `derived/` | 有验证证据和适用边界 |
| 失败后修复 | `fixes/` | 有失败原因、修复响应、新证据 |
| 临时观察 | `captured/` | 标注 confidence，不升级为规则 |
| 系统性治理失误 | `scars/` | 必须说明触发条件、禁止动作、恢复动作 |
| 无沉淀价值 | 不写回 | 在 `evolutionWritebackPacket` 写 `none + reason` |

写回必须经过 `evolutionGate`，禁止静默跳过。

---

## 六、v7.0 生命周期与迁徙

经验文件是证据产物，不因写入而成为生效规则。生命周期事实源为 Runtime：

```text
DRAFT → VALIDATING → APPROVED → ACTIVE
              └────→ QUARANTINED
ACTIVE ────────────→ QUARANTINED / RETIRED
```

| 候选类型 | ACTIVE 投影目标 |
|---|---|
| KNOWLEDGE_ONLY | raw / Wiki；不进入 Evolve 状态机 |
| CONTEXT_RULE | MEMORY.md |
| TOOL_ROUTE | TOOLS.md |
| SKILL | 相关技能及测试 |
| PROJECT_RULE | 项目 AGENTS.md |
| POLICY | 工作区 AGENTS.md 或治理策略 |
| PROMPT / MODEL_ROUTE / GATE / DELIVERY_RULE | 对应 Runtime 配置或门禁 |

硬规则：

- DRAFT 只表示已捕获，不关闭 correction。
- APPROVED 只表示已批准，不等于已部署。
- ACTIVE 后才关闭关联 correction 并投影目标文件。
- 回归时进入 QUARANTINED；回滚恢复上一 ACTIVE。
- AGENTS.md 不再追加原始经验，只接收短、稳定、已激活规则。

---

*本文件是经验存储系统的入口规范*
*实际经验条目存储在对应的子目录中*
