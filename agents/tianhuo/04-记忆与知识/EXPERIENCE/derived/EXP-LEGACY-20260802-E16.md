---
schema: awkn-experience-candidate/v1
experienceId: EXP-LEGACY-20260802-E16
disposition: EVOLVE
candidateType: GATE
status: DRAFT
legacySource: "D:/awkn-lab/AGENTS.md#E16"
sourceEvidence:
  - "D:/awkn-lab/awkn引擎/agents/tianhuo/04-记忆与知识/EXPERIENCE/raw/2026-08-02-file-AGENTS-legacy-E1-E26.md#E16"
scope: "仅限原经验明确覆盖的系统、平台、工具与项目条件"
proposedTarget: "Runtime Gate 配置与自动化测试"
verification: "构造通过、拒绝、崩溃恢复三类测试；验证 fail-closed 且失败可见。"
expiryConditions: "Gate 实现、事件模型、CI 平台或锁语义变化时失效。"
---

# EXP-LEGACY-20260802-E16 — E16 CI/CD 工具文件必须 git tracked

## 候选结论

pipeline 定义（cicd.json）、guard 脚本（cicd-guards.js）等 CI/CD 基础设施文件必须 `git add` + `git commit`，不能只作为本地文件存在。`git clean` 会删除未跟踪文件，导致 hook 静默失败、所有后续提交绕过 CI 验证。

证据：`.awkn/actions/cicd.json` 和 `scripts/cicd-guards.js` 从未被 git 跟踪，v21 工作期间 `git clean` 删除后 post-commit hook 对 253 个提交静默失败。

**触发词**：cicd.json / git clean / 未跟踪 / hook 失败 / 工具文件 / Pipeline definition not found

## 迁徙声明

- 此条目从历史 AGENTS 经验正文迁出。
- 历史存在不代表已经通过当前 Runtime 回放。
- 当前状态为 DRAFT，不得被 Agent 当作生效规则加载。
- 激活前必须补齐可执行变更 manifest、基线、反例和独立验证证据。
