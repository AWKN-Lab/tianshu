---
schema: awkn-experience-candidate/v1
experienceId: EXP-LEGACY-20260802-E18
disposition: EVOLVE
candidateType: TOOL_ROUTE
status: DRAFT
legacySource: "D:/awkn-lab/AGENTS.md#E18"
sourceEvidence:
  - "D:/awkn-lab/awkn引擎/agents/tianhuo/04-记忆与知识/EXPERIENCE/raw/2026-08-02-file-AGENTS-legacy-E1-E26.md#E18"
scope: "仅限原经验明确覆盖的系统、平台、工具与项目条件"
proposedTarget: "TOOLS.md 或对应工具适配器"
verification: "在声明的 OS、shell、工具版本中执行正反例；验证命令、路径和失败诊断。"
expiryConditions: "工具版本、OS、shell、配置入口或命令语义变化时失效。"
---

# EXP-LEGACY-20260802-E18 — E18 EventStore stale lock 需手动清除

## 候选结论

awkn-action-runner 用 SQLite EventStore 跟踪 active runs。如果 run 进程崩溃未过渡到终止状态（stuck in 'running'），会永久阻塞同 workflow 的所有后续 run。清除方法：直接更新 SQLite 数据库 `UPDATE runs SET status='failed' WHERE id='<stale_run_id>'`。

数据库路径：`d:\awkn-lab\awkn引擎\runtime\data\awkn-engine.db`（或 `$AWKN_DB_PATH`）。

证据：run cae518f2 stuck in 'running' 状态，阻塞了 31f0286 的 pipeline 触发。手动 UPDATE 后 pipeline 立即成功启动。

**触发词**：another active run / rejected / stale lock / EventStore / awkn-engine.db / pipeline blocked

## 迁徙声明

- 此条目从历史 AGENTS 经验正文迁出。
- 历史存在不代表已经通过当前 Runtime 回放。
- 当前状态为 DRAFT，不得被 Agent 当作生效规则加载。
- 激活前必须补齐可执行变更 manifest、基线、反例和独立验证证据。
