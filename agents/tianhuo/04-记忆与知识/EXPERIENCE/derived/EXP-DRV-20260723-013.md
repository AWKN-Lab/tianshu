# EXP-DRV-20260723-013 — 自进化闭环第二处断链：derived 草稿补全从未触发

- **类型**: derived（系统自动检测 + 人工提炼）
- **日期**: 2026-07-23
- **触发**: 验证自进化闭环最后一环连通性
- **严重度**: 高（草稿永远堆积，自进化闭环卡在"起草"阶段）
- **状态**: 已提炼

## 1. 问题概述

M3 进阶-17 修复了 record() 断链后，继续追踪闭环的最后一环：

```
Error → record() → corrections-ledger → pattern-detector → experience-writer 写草稿 → [???] → 草稿补全
```

**发现**：experience-writer 写 derived 草稿（含"待人工补充"标记），awkn-复盘总结 SKILL.md 15.1 文档化了补全流程，但 **没有任何代码触发该流程**：

- 无 cron job 定期扫描 derived/
- 无 session_stop hook 触发草稿补全
- 无 CLI 命令触发 15.1 流程
- prd-centric-loop.ts:190 调用 awkn-复盘总结，但传的是"复盘"prompt，不是"补全草稿"prompt

**结果**：derived 草稿永远堆积，自进化闭环卡在"起草"阶段。

## 2. 根因分析

与 M3 进阶-17 完全同类的"无输入被当作已处理"模式，但发生在 pipeline 的**出口端**：

| 维度 | M3 进阶-17（入口断链） | M3 进阶-18（出口断链） |
|------|----------------------|----------------------|
| 断点位置 | record() 从未被调用 | completePendingDrafts() 从未存在 |
| 表面现象 | 四件套全部实现 | 15.1 流程已文档化 |
| 实际问题 | pipeline 入口无数据 | pipeline 出口无触发 |
| 为什么没发现 | 各组件单元测试只验证自身 | 只验证了"写草稿"，没验证"补全草稿" |

**关键教训**：修复一处断链后，必须继续验证 pipeline 后续环节 — 否则只修了第一个 gap，后面还有更多。

## 3. 修复方案

### M3 进阶-18：添加 derived 草稿补全触发

**1. `experience-writer.ts`** — 新增两个函数：
- `scanPendingDrafts()` — 纯文件扫描，不需要 LLM，扫描 derived/ 含"待人工补充"标记的文件
- `completePendingDrafts(cwd)` — 调 awkn-复盘总结 技能补全草稿（需要 LLM），检查 terminated 不假成功

**2. `cli.ts`** — 新增 2 个子命令：
- `evolve scan-drafts` — 扫描待补全草稿（不需要 LLM）
- `evolve complete-drafts` — 触发 15.1 补全流程（需要 LLM）

### 验证

verify-evolve-loop.ts 新增 5 项测试（共 16 项）：
1. 静态：experience-writer.ts 含 scanPendingDrafts + completePendingDrafts
2. 静态：cli.ts 含 scan-drafts / complete-drafts 子命令
3. 端到端：scanPendingDrafts 找到待补全草稿（跳过已完成的）
4. 端到端：scanPendingDrafts 无草稿时返回空数组
5. 端到端：writeAllExperiences 后 scanPendingDrafts 能找到新生成的草稿

## 4. 与 E95 的关系

E95 说"pipeline 必须有端到端连通性验证"。M3 进阶-18 是 E95 的**第二处实例**，补充了一个重要教训：

**E95 补充**：端到端连通性验证必须覆盖 pipeline 的**每一个环节**，不只是第一个发现的 gap。修复一处断链后，必须继续验证后续环节 — 否则只修了入口，出口仍然断着。

## 5. 自进化闭环完整性验证（M3 进阶-17 + 18 修复后）

```
[入口] gate FAIL / loop_monitor 失败
  ↓ recordGateFailures() / recordLoopFailure()     ← M3 进阶-17 修复
[存储] corrections-ledger
  ↓ pattern-detector.detect()
[检测] DetectedPattern[]
  ↓ writeAllExperiences()
[起草] derived/EXP-DRV-*.md（含"待人工补充"标记）
  ↓ scanPendingDrafts() + completePendingDrafts()  ← M3 进阶-18 修复
[补全] awkn-复盘总结 15.1 流程（LLM 补全根因 + 铁律）
  ↓
[完成] derived/EXP-DRV-*.md（状态改为"已提炼"）
```

**入口 → 存储 → 检测 → 起草 → 补全 → 完成** — 全链路连通（补全环节需要 LLM，当前被 key 阻塞但触发机制已就位）。

## 6. 解决方案

- [x] 补充根因分析
- [x] 添加 scanPendingDrafts() 纯文件扫描
- [x] 添加 completePendingDrafts() 调技能补全
- [x] 添加 CLI 命令 scan-drafts / complete-drafts
- [x] 端到端验证（5 项新增测试通过）

---

_本文件由人工分析生成。M3 进阶-18 修复完成。_
