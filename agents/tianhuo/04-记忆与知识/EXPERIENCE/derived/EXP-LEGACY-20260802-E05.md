---
schema: awkn-experience-candidate/v1
experienceId: EXP-LEGACY-20260802-E05
disposition: EVOLVE
candidateType: GATE
status: DRAFT
legacySource: "D:/awkn-lab/AGENTS.md#E5"
sourceEvidence:
  - "D:/awkn-lab/awkn引擎/agents/tianhuo/04-记忆与知识/EXPERIENCE/raw/2026-08-02-file-AGENTS-legacy-E1-E26.md#E5"
scope: "仅限原经验明确覆盖的系统、平台、工具与项目条件"
proposedTarget: "Runtime Gate 配置与自动化测试"
verification: "构造通过、拒绝、崩溃恢复三类测试；验证 fail-closed 且失败可见。"
expiryConditions: "Gate 实现、事件模型、CI 平台或锁语义变化时失效。"
---

# EXP-LEGACY-20260802-E05 — E5 schema-gate 切换前先验证 DB 角色分离

## 候选结论

任何 `start.js` 类 schema-gate 入口，切换前必须确认 `SCHEMA_CHECK_DB_*` 与 `APP_DB_*` 已按最小权限原则分离。未分离时直接切换会导致启动失败，需先创建只读的审计用户（或按架构要求的最小权限用户）。

**触发词**：schema-gate / SCHEMA_CHECK_DB / start.js / 入口切换 / migration gate

## 迁徙声明

- 此条目从历史 AGENTS 经验正文迁出。
- 历史存在不代表已经通过当前 Runtime 回放。
- 当前状态为 DRAFT，不得被 Agent 当作生效规则加载。
- 激活前必须补齐可执行变更 manifest、基线、反例和独立验证证据。
