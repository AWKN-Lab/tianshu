---
schema: awkn-experience-candidate/v1
experienceId: EXP-DLG-20260802-002
disposition: EVOLVE
candidateType: PROJECT_RULE
status: DRAFT
sourceEvidence:
  - "对话复盘: env 污染修复闭环"
  - "commits 57b5d41, 92641aa"
lesson: "测试读 env 必须 beforeEach 清理（save/restore 范式）；afterEach-only 清理使首用例裸奔于宿主 env。测试自给自足（自设自删）才是根治，进程级净化是第二道防线。"
scope: "本仓库 runtime/test 所有涉 env 测试；未来新增读 env 的测试"
proposedTarget: "项目 AGENTS.md（测试规范）或 run-tests 工程文档"
verification: "新增读 env 测试按此规范编写后，污染宿主 env 直跑仍全绿"
expiryConditions: "测试运行机制彻底重构（如 runner 自带隔离）时重新验证"
---

# EXP-DLG-20260802-002：测试读 env 必须自给自足

## 候选要点

1. 读 env 的测试必须 `beforeEach` 保存/恢复，`afterEach`-only 会让第一个用例读到宿主 env。
2. 进程级净化（run-tests.mjs 剔除 AWKN_*）是兜底，不是根治；测试自给自足才是根治。

## 迁徙说明

- 生成方式：对话复盘提取（人工总结）
- 当前状态：DRAFT，未经回放验证
- 验证前须补：回放证据、覆盖全部涉 env 测试的清单
