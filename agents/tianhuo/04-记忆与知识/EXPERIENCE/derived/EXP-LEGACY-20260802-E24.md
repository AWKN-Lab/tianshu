---
schema: awkn-experience-candidate/v1
experienceId: EXP-LEGACY-20260802-E24
disposition: EVOLVE
candidateType: TOOL_ROUTE
status: DRAFT
legacySource: "D:/awkn-lab/AGENTS.md#E24"
sourceEvidence:
  - "D:/awkn-lab/awkn引擎/agents/tianhuo/04-记忆与知识/EXPERIENCE/raw/2026-08-02-file-AGENTS-legacy-E1-E26.md#E24"
scope: "仅限原经验明确覆盖的系统、平台、工具与项目条件"
proposedTarget: "TOOLS.md 或对应工具适配器"
verification: "在声明的 OS、shell、工具版本中执行正反例；验证命令、路径和失败诊断。"
expiryConditions: "工具版本、OS、shell、配置入口或命令语义变化时失效。"
---

# EXP-LEGACY-20260802-E24 — E24 日志归档用 cp + truncate 而非 restart

## 候选结论

在线服务日志过大但不允许中断时，用 `cp logfile archive/logfile-$(date)` 备份 + `truncate -s 0 logfile` 清空原文件，而非重启服务。truncate 不改变文件 inode，持有文件句柄的进程（PM2/systemd）继续写入，零中断。

**触发词**：日志归档 / truncate / 零中断 / cp + truncate / 日志过大 / 不重启

## 迁徙声明

- 此条目从历史 AGENTS 经验正文迁出。
- 历史存在不代表已经通过当前 Runtime 回放。
- 当前状态为 DRAFT，不得被 Agent 当作生效规则加载。
- 激活前必须补齐可执行变更 manifest、基线、反例和独立验证证据。
