---
schema: awkn-experience-candidate/v1
experienceId: EXP-LEGACY-20260802-E17
disposition: EVOLVE
candidateType: GATE
status: DRAFT
legacySource: "D:/awkn-lab/AGENTS.md#E17"
sourceEvidence:
  - "D:/awkn-lab/awkn引擎/agents/tianhuo/04-记忆与知识/EXPERIENCE/raw/2026-08-02-file-AGENTS-legacy-E1-E26.md#E17"
scope: "仅限原经验明确覆盖的系统、平台、工具与项目条件"
proposedTarget: "Runtime Gate 配置与自动化测试"
verification: "构造通过、拒绝、崩溃恢复三类测试；验证 fail-closed 且失败可见。"
expiryConditions: "Gate 实现、事件模型、CI 平台或锁语义变化时失效。"
---

# EXP-LEGACY-20260802-E17 — E17 异步 hook 必须有失败可见性

## 候选结论

post-commit hook 用 `nohup ... > log 2>&1 &` 后台运行时，必须增加 pre-check（验证关键文件存在）+ 失败时 stderr 告警。否则 hook 静默失败，提交者完全无感知，形成运维盲区。

证据：hook 失败 253 次无人发现，因为 `nohup &` 的输出只写入日志文件，不输出到终端。修复后增加 `if [ ! -f "$PIPELINE_DEF" ]; then echo "⚠️ PIPELINE_HOOK_FAILED" >&2; fi`。

**触发词**：nohup / 静默失败 / post-commit / stderr / hook 可见性 / 异步任务

## 迁徙声明

- 此条目从历史 AGENTS 经验正文迁出。
- 历史存在不代表已经通过当前 Runtime 回放。
- 当前状态为 DRAFT，不得被 Agent 当作生效规则加载。
- 激活前必须补齐可执行变更 manifest、基线、反例和独立验证证据。
