---
schema: awkn-experience-candidate/v1
experienceId: EXP-LEGACY-20260802-E20
disposition: EVOLVE
candidateType: PROJECT_RULE
status: DRAFT
legacySource: "D:/awkn-lab/AGENTS.md#E20"
sourceEvidence:
  - "D:/awkn-lab/awkn引擎/agents/tianhuo/04-记忆与知识/EXPERIENCE/raw/2026-08-02-file-AGENTS-legacy-E1-E26.md#E20"
scope: "仅限原经验明确覆盖的系统、平台、工具与项目条件"
proposedTarget: "适用项目 AGENTS.md 的受管投影"
verification: "在适用项目的历史失败集与成功基线中回放，并运行对应自动化测试。"
expiryConditions: "项目架构、schema、平台或适用路径发生变化时重新验证。"
---

# EXP-LEGACY-20260802-E20 — E20 构建工作区不应放 /opt/

## 候选结论

构建工作区（如 mrmont-v2-build）应放在 `/tmp/` 或带自动清理的 workspace 目录，不能放 `/opt/`。`/opt/` 被当作永久目录，临时构建产物堆积在此不会被 cron 清理。`/tmp/` 有定时清理（`find /tmp -name "*.bundle" -mtime +1 -delete`），但 `/opt/` 没有。

**触发词**：构建工作区 / mrmont / /opt 清理 / 构建产物 / 临时目录

## 迁徙声明

- 此条目从历史 AGENTS 经验正文迁出。
- 历史存在不代表已经通过当前 Runtime 回放。
- 当前状态为 DRAFT，不得被 Agent 当作生效规则加载。
- 激活前必须补齐可执行变更 manifest、基线、反例和独立验证证据。
