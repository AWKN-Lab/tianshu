# EXP-DRV-20260804-011 — install-mcp-config.mjs IDE 清单维护

- **类型**: derived
- **日期**: 2026-08-04T11:05:00.000Z
- **状态**: 待人工补全
- **fingerprint**: install-mcp-config-ide-list-20260804
- **来源 evidence**: 2026-08-04 Qoder MCP 接入会话 + install-mcp-config.mjs 增 qoder 目标

## 1. 检测到的模式

| 维度 | 值 |
|---|---|
| kind | ide-adapter-gap |
| source | mcp-installer |
| count | 1 |
| first_ts | 2026-08-04T07:00:00.000Z |
| last_ts | 2026-08-04T07:05:00.000Z |
| goal_id | Qoder MCP 接入 |

## 2. 最近一次错误文本

```
Qoder 全局 MCP 配置 mcp.json = {"mcpServers": {}} —— 空的
awkn-engine 工具未出现在 IDE 工具列表中
```

## 3. 样本事件 ID

- Qoder-MCP-20260804-001

## 4. 根因分析（待人工补充）

- 根因：install-mcp-config.mjs IDE 目标清单扩展时遗漏 qoder
- 触发条件：项目接入新 IDE 但 MCP 安装器未更新
- 排除路径：仅适用 MCP 适配；非 MCP 路线无效
- 影响：awkn-engine 工具不可见，内存接入断裂

## 5. 待提炼的铁律（待人工补全）

1. 任何新 IDE 接入前必须先查 install-mcp-config.mjs 的 IDE 清单
2. 缺失则先补，再执行 `node scripts/install-mcp-config.mjs --ide <name>`
3. macOS/Linux 走 `~/.qoder/mcp.json`，Windows 走 `%APPDATA%\QoderCN\SharedClientCache\mcp.json`

## 6. 晋级条件

- [ ] 成功率不下降
- [ ] 循环数与 Token 不越过阈值
- [ ] 错误率、人工接管率、安全违规率不增加
- [ ] 通过后进入 APPROVED，再由激活动作进入 ACTIVE

---

_自动生成的候选经验（DRAFT）。相关 correction 在候选 ACTIVE 前保持 open；待 awkn-复盘总结补全根因与工程规则，并经回放、人工批准后晋级。_
