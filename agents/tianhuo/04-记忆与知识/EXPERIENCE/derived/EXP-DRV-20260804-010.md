# EXP-DRV-20260804-010 — PS 5.1 写 .ps1 副本必须带 BOM（UTF-8）

- **类型**: derived
- **日期**: 2026-08-04T11:00:00.000Z
- **状态**: 待人工补充
- **fingerprint**: powershell-5.1-bom-20260804
- **来源 evidence**: 2026-08-04 深度复盘报告 §6 5Why P1 / Memory OS AutoStart 任务计划手动触发 LastTaskResult=1

## 1. 检测到的模式

| 维度 | 值 |
|---|---|
| kind | reviewGate_failure |
| source | engine-command-runner |
| count | 3 |
| first_ts | 2026-08-04T08:00:00.000Z |
| last_ts | 2026-08-04T09:30:00.000Z |
| goal_id | AWKN MemoryOS AutoStart |

## 2. 最近一次错误文本

```
ParserError: 缺少带 BOM 的 UTF-8 标记，PowerShell 5.1 按 ANSI 解析，
  中文字符被截断为非法 token → 退出码 1
```

## 3. 样本事件 ID

- AWKN-local-20260804-001
- AWKN-local-20260804-002
- AWKN-local-20260804-003

## 4. 根因分析（待人工补充）

- 5Why 根因：Windows PS 5.1 与 PS 7.0+ 默认编码不同（PS 5.1 按 ANSI，PS 7.0+ 按 UTF-8）
- 触发条件：.ps1 文件无 BOM 且包含 非 ASCII 字符
- 排除路径：仅 PS 5.1 解析时；PS 7.0+ 默认 UTF-8 不需要 BOM
- 来源：runtime/data/awkn-engine.db 任务计划 lastTaskResult=1

## 5. 待提炼的铁律（待人工补充）

1. 写 .ps1 副本必须用 `[System.IO.File]::WriteAllText(..., [System.Text.UTF8Encoding]::new($true))` 强制带 BOM
2. 脚本正文优先改英文，避免跨 PS 版本差异
3. 任何 PowerShell 任务计划部署后必须手动触发验证 LastTaskResult=0

## 6. 晋级条件

- [ ] 成功率不下降
- [ ] 循环数与 Token 不越过阈值
- [ ] 错误率、人工接管率、安全违规率不增加
- [ ] 通过后进入 APPROVED，再由激活动作进入 ACTIVE

---

_自动生成的候选经验（DRAFT）。相关 correction 在候选 ACTIVE 前保持 open；待 awkn-复盘总结补全根因与工程规则，并经回放、人工批准后晋级。_
