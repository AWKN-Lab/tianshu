---
schema: awkn-experience-candidate/v1
experienceId: EXP-LEGACY-20260802-E10
disposition: EVOLVE
candidateType: DELIVERY_RULE
status: DRAFT
legacySource: "D:/awkn-lab/AGENTS.md#E10"
sourceEvidence:
  - "D:/awkn-lab/awkn引擎/agents/tianhuo/04-记忆与知识/EXPERIENCE/raw/2026-08-02-file-AGENTS-legacy-E1-E26.md#E10"
scope: "仅限原经验明确覆盖的系统、平台、工具与项目条件"
proposedTarget: "Runtime 交付策略与门禁"
verification: "在隔离发布环境执行成功路径、失败注入、精确制品身份和回滚验证。"
expiryConditions: "部署平台、制品格式、发布流程或运行环境变化时失效。"
---

# EXP-LEGACY-20260802-E10 — E10 SHA256SUMS 必须从 Git blobs 生成，不能从工作区字节生成

## 候选结论

完整性校验清单（SHA256SUMS.txt）必须从 `git cat-file blob HEAD:<path>` 生成，而非直接读取工作区文件字节。Windows 上 `core.autocrlf=true` 会导致工作区文件含 CRLF，而 Git blob 存储的是规范化后的内容（LF），两者哈希不一致。跨机器验证时工作区字节哈希无法稳定复现。

**触发词**：SHA256 / checksum / 完整性 / CRLF / 跨机器 / 哈希不一致 / autocrlf

## 迁徙声明

- 此条目从历史 AGENTS 经验正文迁出。
- 历史存在不代表已经通过当前 Runtime 回放。
- 当前状态为 DRAFT，不得被 Agent 当作生效规则加载。
- 激活前必须补齐可执行变更 manifest、基线、反例和独立验证证据。
