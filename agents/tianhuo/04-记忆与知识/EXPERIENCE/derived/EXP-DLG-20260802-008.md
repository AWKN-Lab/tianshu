---
schema: awkn-experience-candidate/v1
experienceId: EXP-DLG-20260802-008
disposition: EVOLVE
candidateType: GATE
status: DRAFT
sourceEvidence:
  - "对话复盘: 收口执行闭环（push 前双重敏感扫描）"
lesson: "push/发布前敏感扫描要双层：① 提交 diff（git log -p）② 未跟踪归档文件（raw 等）。第一层扫描曾因 remote 名错误失效，第二层（raw 文件）兜住。扫描命中需人工判别（脱敏测试样例 vs 真实凭据）。"
scope: "任何 push/发布/归档前；含未跟踪文件的工作区"
proposedTarget: "awkn-git 技能（push 前门禁）或 CI 门禁"
verification: "push 前自动双层扫描且命中需人工确认"
expiryConditions: "接入专用 secret scanner（如 gitleaks/trufflehog）时失效"
---

# EXP-DLG-20260802-008：push 前双层敏感扫描

## 候选要点

- 第一层：提交 diff（`git log -p remote/main..HEAD`）。
- 第二层：未跟踪/归档文件（raw 目录等，不进 diff 的路径）。
- 命中不可自动放过：脱敏样例（AKIAIOSFODNN7EXAMPLE、hunter2）须与真实凭据区分。

## 迁徙说明

- 生成方式：对话复盘提取（人工总结）
- 当前状态：DRAFT，未经回放验证
- 验证前须补：回放证据（含未跟踪文件场景的 push）
