---
schema: awkn-experience-candidate/v1
experienceId: EXP-LEGACY-20260802-E08
disposition: EVOLVE
candidateType: TOOL_ROUTE
status: DRAFT
legacySource: "D:/awkn-lab/AGENTS.md#E8"
sourceEvidence:
  - "D:/awkn-lab/awkn引擎/agents/tianhuo/04-记忆与知识/EXPERIENCE/raw/2026-08-02-file-AGENTS-legacy-E1-E26.md#E8"
scope: "仅限原经验明确覆盖的系统、平台、工具与项目条件"
proposedTarget: "TOOLS.md 或对应工具适配器"
verification: "在声明的 OS、shell、工具版本中执行正反例；验证命令、路径和失败诊断。"
expiryConditions: "工具版本、OS、shell、配置入口或命令语义变化时失效。"
---

# EXP-LEGACY-20260802-E08 — E8 pm2 reload <name> 不重新读取 ecosystem.config.js

## 候选结论

要让 PM2 重新读取 `ecosystem.config.js` 必须用完整路径：`pm2 reload /opt/tut/ecosystem.config.js --only tut-backend`。仅 `pm2 reload tut-backend` 会更新进程但不重读配置文件。

**触发词**：pm2 reload / ecosystem.config.js / env 不生效 / 配置未更新

## 迁徙声明

- 此条目从历史 AGENTS 经验正文迁出。
- 历史存在不代表已经通过当前 Runtime 回放。
- 当前状态为 DRAFT，不得被 Agent 当作生效规则加载。
- 激活前必须补齐可执行变更 manifest、基线、反例和独立验证证据。
