# EXP-DRV-20260804-013 — memory-cli 诊断必须断言 JSON 模式而非字符串存在

- **类型**: derived
- **日期**: 2026-08-04T11:15:00.000Z
- **状态**: 待人工补充
- **fingerprint**: doctor-json-assert-20260804
- **来源 evidence**: 2026-08-04 深度复盘报告 §4 假设 A4（被代码证伪）

## 1. 检测到的模式

| 维度 | 值 |
|---|---|
| kind | silent-degradation |
| source | memory-cli |
| count | 1 |
| first_ts | 2026-08-04T05:00:00.000Z |
| last_ts | 2026-08-04T05:30:00.000Z |
| goal_id | Memory OS 连接 |

## 2. 最近一次错误文本

```
memory-cli.ts 原本不调 loadRuntimeEnv() → .env 字符串在但运行时仍 local
doctor 验证仅检查 .env 字符串存在 → 静默误判
```

## 3. 样本事件 ID

- MemoryOS-doctor-20260804-001

## 4. 根因分析（待人工补充）

- 根因：CLI 工具默认未调 loadRuntimeEnv → 配置文件被解析但运行时仍走 local fallback
- 触发条件：CLI 直接 spawn 不经 mcp/server.ts
- 排除路径：mcp/server.ts 已自动调用 loadRuntimeEnv
- 影响：doctor 字符串检查会骗过用户，实际仍降级

## 5. 待提炼的铁律（待人工补充）

1. 任何 CLI 工具（memory-cli / evolution-cli / cron-cli）首行必须 `loadRuntimeEnv()`
2. doctor 验证必须断言诊断 JSON 模式：`mode==='memory-os' && remoteEnabled && remote.capabilities.online`
3. 不要只检查 .env 字符串存在

## 6. 晋级条件

- [ ] 成功率不下降
- [ ] 循环数与 Token 不越过阈值
- [ ] 错误率、人工接管率、安全违规率不增加
- [ ] 通过后进入 APPROVED，再由激活动作进入 ACTIVE

---

_自动生成的候选经验（DRAFT）。相关 correction 在候选 ACTIVE 前保持 open；待 awkn-复盘总结补全根因与工程规则，并经回放、人工批准后晋级。_
