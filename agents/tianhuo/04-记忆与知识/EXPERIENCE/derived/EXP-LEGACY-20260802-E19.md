---
schema: awkn-experience-candidate/v1
experienceId: EXP-LEGACY-20260802-E19
disposition: EVOLVE
candidateType: DELIVERY_RULE
status: DRAFT
legacySource: "D:/awkn-lab/AGENTS.md#E19"
sourceEvidence:
  - "D:/awkn-lab/awkn引擎/agents/tianhuo/04-记忆与知识/EXPERIENCE/raw/2026-08-02-file-AGENTS-legacy-E1-E26.md#E19"
scope: "仅限原经验明确覆盖的系统、平台、工具与项目条件"
proposedTarget: "Runtime 交付策略与门禁"
verification: "在隔离发布环境执行成功路径、失败注入、精确制品身份和回滚验证。"
expiryConditions: "部署平台、制品格式、发布流程或运行环境变化时失效。"
---

# EXP-LEGACY-20260802-E19 — E19 服务器清理三步备份法（tar + scp + SHA256）

## 候选结论

服务器删除 >10M 目录前必须执行三步备份：① 服务端 `tar -czf` 打包（排除 `.git/`、`node_modules/`、`vendor/`）→ ② `scp` 下载到本地 → ③ 双端 `sha256sum` 校验一致。校验通过后才 `rm -rf`，服务端 tar 包在本地校验后立即删除避免二次占用。

**触发词**：服务器清理 / rm -rf / 删除目录 / 备份 / 磁盘空间 / tar scp

## 迁徙声明

- 此条目从历史 AGENTS 经验正文迁出。
- 历史存在不代表已经通过当前 Runtime 回放。
- 当前状态为 DRAFT，不得被 Agent 当作生效规则加载。
- 激活前必须补齐可执行变更 manifest、基线、反例和独立验证证据。
