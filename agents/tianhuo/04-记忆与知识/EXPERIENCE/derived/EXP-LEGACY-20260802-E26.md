---
schema: awkn-experience-candidate/v1
experienceId: EXP-LEGACY-20260802-E26
disposition: EVOLVE
candidateType: PROJECT_RULE
status: DRAFT
legacySource: "D:/awkn-lab/AGENTS.md#E26"
sourceEvidence:
  - "D:/awkn-lab/awkn引擎/agents/tianhuo/04-记忆与知识/EXPERIENCE/raw/2026-08-02-file-AGENTS-legacy-E1-E26.md#E26"
scope: "仅限原经验明确覆盖的系统、平台、工具与项目条件"
proposedTarget: "适用项目 AGENTS.md 的受管投影"
verification: "在适用项目的历史失败集与成功基线中回放，并运行对应自动化测试。"
expiryConditions: "项目架构、schema、平台或适用路径发生变化时重新验证。"
---

# EXP-LEGACY-20260802-E26 — E26 时间相关测试失败先算偏移量

## 候选结论

时间相关断言失败时（`expected X to be less than or equal to Y`），第一步计算 `X - Y` 的偏移量：
- 偏移 = 8h（28800000ms）→ 时区 bug（UTC vs localtime 混用）
- 偏移 = 1h（3600000ms）→ DST 或时区边界
- 偏移 = 数小时且不稳定 → `Date.now()` 在不同代码路径的调用时差
- 偏移 = 固定常量 → 硬编码的 grace period 或 buffer

不要急于改测试断言，先定位偏移来源。

**触发词**：时间断言失败 / toBeLessThanOrEqual / 偏移量 / 28800000 / 时区 bug 定位 / 时间相关测试

## 迁徙声明

- 此条目从历史 AGENTS 经验正文迁出。
- 历史存在不代表已经通过当前 Runtime 回放。
- 当前状态为 DRAFT，不得被 Agent 当作生效规则加载。
- 激活前必须补齐可执行变更 manifest、基线、反例和独立验证证据。
