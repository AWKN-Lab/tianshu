---
schema: awkn-experience-candidate/v1
experienceId: EXP-LEGACY-20260802-E23
disposition: EVOLVE
candidateType: TOOL_ROUTE
status: DRAFT
legacySource: "D:/awkn-lab/AGENTS.md#E23"
sourceEvidence:
  - "D:/awkn-lab/awkn引擎/agents/tianhuo/04-记忆与知识/EXPERIENCE/raw/2026-08-02-file-AGENTS-legacy-E1-E26.md#E23"
scope: "仅限原经验明确覆盖的系统、平台、工具与项目条件"
proposedTarget: "TOOLS.md 或对应工具适配器"
verification: "在声明的 OS、shell、工具版本中执行正反例；验证命令、路径和失败诊断。"
expiryConditions: "工具版本、OS、shell、配置入口或命令语义变化时失效。"
---

# EXP-LEGACY-20260802-E23 — E23 inode 访问法处理异常文件名

## 候选结论

服务器上因误操作产生的异常文件名（如 `\`、`\ `、`你再试试之前没成功的工具`）无法用常规引号路径访问时，用 `find /root -maxdepth 1 -inum <inode> -exec <command> {} \;` 通过 inode 号操作。`find -inum` + `-exec` 或 `-delete` 是处理任意文件名的万能方法。

**触发词**：异常文件名 / 反斜杠 / inode / find -inum / 文件名删除 / cat -v

## 迁徙声明

- 此条目从历史 AGENTS 经验正文迁出。
- 历史存在不代表已经通过当前 Runtime 回放。
- 当前状态为 DRAFT，不得被 Agent 当作生效规则加载。
- 激活前必须补齐可执行变更 manifest、基线、反例和独立验证证据。
