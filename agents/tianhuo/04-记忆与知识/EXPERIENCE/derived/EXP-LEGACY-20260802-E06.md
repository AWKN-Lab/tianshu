---
schema: awkn-experience-candidate/v1
experienceId: EXP-LEGACY-20260802-E06
disposition: EVOLVE
candidateType: TOOL_ROUTE
status: DRAFT
legacySource: "D:/awkn-lab/AGENTS.md#E6"
sourceEvidence:
  - "D:/awkn-lab/awkn引擎/agents/tianhuo/04-记忆与知识/EXPERIENCE/raw/2026-08-02-file-AGENTS-legacy-E1-E26.md#E6"
scope: "仅限原经验明确覆盖的系统、平台、工具与项目条件"
proposedTarget: "TOOLS.md 或对应工具适配器"
verification: "在声明的 OS、shell、工具版本中执行正反例；验证命令、路径和失败诊断。"
expiryConditions: "工具版本、OS、shell、配置入口或命令语义变化时失效。"
---

# EXP-LEGACY-20260802-E06 — E6 PowerShell SSH heredoc 不可直接写复杂命令

## 候选结论

Windows PowerShell 通过 `ssh host "..."` 执行 heredoc 或多层引号命令时，会因转义语义差异失败。复杂命令应写入 `.sh` 文件，scp 上传后用 `bash file.sh` 执行。

**触发词**：PowerShell / ssh heredoc / 转义 / EPIPE / Windows 到 Linux

## 迁徙声明

- 此条目从历史 AGENTS 经验正文迁出。
- 历史存在不代表已经通过当前 Runtime 回放。
- 当前状态为 DRAFT，不得被 Agent 当作生效规则加载。
- 激活前必须补齐可执行变更 manifest、基线、反例和独立验证证据。
