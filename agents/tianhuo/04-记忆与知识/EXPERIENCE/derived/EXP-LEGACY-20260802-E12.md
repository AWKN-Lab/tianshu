---
schema: awkn-experience-candidate/v1
experienceId: EXP-LEGACY-20260802-E12
disposition: EVOLVE
candidateType: PROJECT_RULE
status: DRAFT
legacySource: "D:/awkn-lab/AGENTS.md#E12"
sourceEvidence:
  - "D:/awkn-lab/awkn引擎/agents/tianhuo/04-记忆与知识/EXPERIENCE/raw/2026-08-02-file-AGENTS-legacy-E1-E26.md#E12"
scope: "仅限原经验明确覆盖的系统、平台、工具与项目条件"
proposedTarget: "适用项目 AGENTS.md 的受管投影"
verification: "在适用项目的历史失败集与成功基线中回放，并运行对应自动化测试。"
expiryConditions: "项目架构、schema、平台或适用路径发生变化时重新验证。"
---

# EXP-LEGACY-20260802-E12 — E12 PowerShell .ps1 文件必须强制 CRLF 行尾

## 候选结论

`.gitattributes` 必须包含 `*.ps1 text eol=crlf`。PowerShell 解析器对 LF 行尾的 `.ps1` 文件行为异常：`Test-Path` 参数绑定失败（"Cannot bind argument to parameter 'Path' because it is null"）、行号报告错误。LF 行尾的脚本在 Windows 上可能静默失败或产生难以定位的错误。

**触发词**：.ps1 / CRLF / Test-Path / 参数绑定失败 / LF / PowerShell 行尾 / .gitattributes

## 迁徙声明

- 此条目从历史 AGENTS 经验正文迁出。
- 历史存在不代表已经通过当前 Runtime 回放。
- 当前状态为 DRAFT，不得被 Agent 当作生效规则加载。
- 激活前必须补齐可执行变更 manifest、基线、反例和独立验证证据。
