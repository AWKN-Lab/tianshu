# ChatGPT 5.6 Thinking 外部工程审查任务书

## 1. 背景与目标

你是本次双代理流程中的外部高级工程师。请基于随附源码 ZIP 对 `AWKN-Lab/tianshu`（本地目录名 `awkn引擎`）进行批判性、源码级审计，并在证据支持的范围内准备最小、完整、可应用的修复。

核心问题：

1. 当前实现是否已经形成“自主执行 → 自动循环 → 证据门禁 → 结果交付 → Memory Write → 失败经验 → 评测 → 技能/规则晋级 → 后续运行生效”的真实闭环？
2. Skill 系统是否真正支持基于 Outcome/Replay 的优化与进化，还是仅有加载、匹配、候选或草稿生成？
3. TRAE 与 Codex 等 IDE 是否得到完整宿主集成，还是仅存在 API provider、hooks 格式或文件桥接兼容层？
4. 天枢与 AWKN Memory OS 是否已经按协议完成可运行闭环，还是仍停留在单仓 adapter、mock/fixture 或 vNext RFC 设计阶段？
5. 当前源码中有哪些会导致假成功、死代码、相对路径错位、静默降级、未持久化状态、无法恢复或错误宣称完成的 P0/P1 缺陷？

不要预设答案。每个结论必须给出文件路径、符号/代码位置、调用链或测试证据。

## 2. 源码与 Git 基线

- ZIP 包含当前工作树，而不只是已提交 HEAD。
- 当前分支：`codex/phase6-runtime-foundation`
- 评审快照基线提交：`e4bc46e553a9bdca41d32c23cef08359efd19a87`
- 最新已拉取远程 `tianshu/main`：`b0312e0ab77ba543e5c7e78913dc8d66360a8c89`
- 分支关系：评审快照基线相对 `tianshu/main` 为 `+4/-0`
- 当前工作树已有用户改动，集中在 Policy/Skill Compiler、Evidence-Gain Loop、Memory OS 验证脚本等。不得假设这些改动已提交或正确。
- 源码包 SHA-256：`88c4c23b4ba99ae093f82df1580279aacbc7ce78af963f9cd7d5aeb5c9adc813`
- 源码包大小：`1,405,993 bytes`

已知本地基线门禁（以评审快照最终状态为准）：

- Architecture Scan：通过，`blockingViolations=0`；仍报告 `directDbImports=20`、`moduleSingletons=12`、`crossComponentImports=22`、`legacyExceptionsUsed=1`。
- 最新 TypeScript typecheck：失败，`runtime/src/contracts/outcome.ts` 中 `SHA256_HEX_PATTERN` 声明后未使用（TS6133）。该文件来自评审前最后一轮并发 C07/C08 工作，请判断应删除、使用或补齐缺失校验。
- lint 当前实际等同 `tsc --noEmit`，因此同样失败；请评价这是否构成真实 lint。
- 最近一次 Unit：`99 passed / 0 failed`（在最后一轮 C07/C08 工作前）。
- 最新 Contract：`640 passed / 0 failed`，共发现 50 个合同测试文件。
- 更早的工作树曾在 `policy-ast-deep.test.ts` 出现 2 个 10 层嵌套真假反转失败；`e4bc46e` 后已消失。请核对修复语义与提交差异，禁止只根据当前绿灯断定逻辑正确。

## 3. 当前架构与不可破坏边界

权威文档入口：

- `README.md`
- `runtime/README.md`
- `docs/agent-os-3.0/README.md`
- `docs/agent-os-3.0/01-核心架构与领域模型.md`
- `docs/agent-os-3.0/05-Policy-Skill-Compiler.md`
- `docs/agent-os-3.0/06-Tool-Model-Broker.md`
- `docs/agent-os-3.0/07-Evidence-Gain-Loop.md`
- `docs/agent-os-3.0/08-Delivery-Evidence-Memory-Evolve.md`
- `docs/agent-os-3.0/09-AWKN-Memory-OS挂载协议.md`
- `docs/agent-os-3.0/17-测试矩阵与Release-Runbook.md`
- `docs/agent-os-3.0/18-Memory-OS-vNext双仓实施RFC.md`
- `docs/agent-os-3.0/21-Agent-OS-3.0总开发计划.md`
- `docs/重构升级设计方案.md`

必须保持：

- Engine v2 仍为默认执行路径；Mode 0、Shadow、Enforce 必须逐组件推进，不得绕过发布状态。
- `ExecutionCoordinator` 是 C01-C09 主链编排器。
- 跨组件只允许 Contracts、`public.ts`、Inbound Port、Domain Event、Receipt Ref。
- 新核心禁止兄弟实现导入、直接 SQLite、模块级可变单例；已有例外必须明确标注。
- Goal 只有 Goal Judge 可以生成 `ACHIEVED`。
- Shadow 禁止真实外部副作用。
- Memory OS 是独立仓库/服务；天枢只通过 `MemoryBackend` 协议挂载，不复制其权威实现。
- 401/403、Project Grant 缺失、协议不兼容必须 fail-closed，禁止用 local fallback 绕过。
- 其他业务仓库不得通过 Memory OS 形成到天枢的传递依赖。
- 多进程 IDE hook → CLI → daemon 的共享路径必须使用绝对路径或显式环境变量，不能依赖 `process.cwd()` 偶然一致。
- 不扩大工具、数据、生产、数据库或发布权限。

## 4. 需要研究和修改的范围

请沿真实调用链审查，而非只看文件是否存在：

1. `goal/`、`core/agent-loop.ts`、`loop/`、`gates/`、`execution/`、`delivery/`、`outcome/` 是否组成可达主链。
2. `evolve/` 的 corrections 入口、pattern detection、experience writer、replay、approval、promotion、quarantine、rollback，及 ACTIVE 资产是否真的进入下一次 Policy/Skill 编译与执行。
3. `skills/manager.ts`、`skills/compiler.ts`、`skills/evaluation-registry.ts` 的外置技能加载、版本、评分、依赖、冲突、预检、历史 Outcome、回放门槛和激活机制。
4. `llm/providers/trae.ts`、`llm/providers/codex.ts`、`llm/router.ts`、`scripts/bridge-daemon.ts`、hook 兼容和 IDE 配置：区分“TRAE IDE 宿主调用”“文件桥”“Codex API 兼容”“Codex IDE 集成”。特别检查桥接目录的 CWD 依赖、daemon provider 循环规避、错误响应、原子写、锁/并发、超时、恢复和凭据边界。
5. `memory/` 全部实现与 Memory OS vNext 文档：协议协商、Descriptor、Grant、Context Receipt/Render、Consume、Transaction CAS、Idempotency、Tombstone、Outbox、Authority、Outcome Attribution、诊断与版本矩阵。必须区分本地 fake server/fixture、单仓 adapter 测试和双仓真实服务 smoke。
6. 当前未跟踪 `runtime/src/loop/`、`runtime/src/contracts/evidence-loop.ts` 及相关测试是否正确接入 public contracts 和执行主链。
7. 架构扫描、CI、package scripts 与测试发现机制是否会漏测 `verify-*.ts`、E2E 或运行脚本；检查当前 `lint` 是否名实相符。
8. README/设计文档中的“已完成”“端到端”“真实”“自主”等宣称是否与当前源码和可复现实验一致。

如发现可以在本仓库内安全修复的 P0/P1 问题，请准备最小完整补丁。若某闭环必须依赖缺失的 Memory OS 仓库、真实 IDE 宿主或外部服务，不要伪造实现；请给出阻塞证据、所需对端契约/测试和本仓库可先完成的 fail-closed 改进。

## 5. 明确交付物

请在一次最终交付中提供：

1. `AUDIT.md`：结论先行，包含证据矩阵。每项使用以下状态之一：`DESIGN_ONLY`、`CODE_PRESENT`、`INTEGRATED`、`MOCK_OR_FIXTURE_ONLY`、`REAL_E2E_VERIFIED`、`BLOCKED_EXTERNAL`。
2. “闭环可达性图”：从输入到下一次运行生效，列出每条调用边及断点。
3. P0/P1/P2 缺陷清单：路径、符号/行、复现方式、影响、正确约束、建议修复。
4. 可应用的最小补丁：优先提供统一 diff；如过大，可提供 changed-files ZIP，并附文件清单、字节数和 SHA-256。补丁必须基于随附工作树，不得回退或覆盖已有改动。
5. `TEST-EVIDENCE.md`：你实际执行的命令、环境、通过/失败/跳过数量和关键日志。未执行必须写“未执行”，不得推测通过。
6. `REMAINING-RISKS.md`：区分本仓库缺口、对端 Memory OS 缺口、真实 TRAE/Codex IDE 验证缺口和生产验证缺口。
7. 文档修正建议：任何过度宣称均改为证据匹配的准确状态。

## 6. 必须执行的测试

若你的沙箱具备 Node 20+：

```text
cd runtime
npm ci
npm run check:architecture
npm run typecheck
npm run lint
npm run test
npm run test:contracts
npm run build
```

并运行与你修改相关的定向测试，以及能证明真实数据流的端到端测试。若无法访问真实 TRAE、Codex IDE 或 Memory OS 服务，必须将相应结论标为未验证或 fixture-only；不得以 mock 替代真实集成证据。

## 7. 禁止执行或禁止声称

- 禁止迁移或修改真实数据库、真实用户数据、真实 Memory OS 服务。
- 禁止部署、发布、推送、创建 PR、修改远程仓库或声称已完成这些操作。
- 禁止要求或使用真实 API Key、Token、Cookie、私钥、验证码。
- 禁止删除、重置或覆盖现有工作树改动。
- 禁止把类型存在、单元测试、mock server、fixture 或手动脚本说成真实生产闭环。
- 禁止把 Codex OpenAI-compatible API provider 直接称为 Codex IDE 集成，除非源码存在并验证了 IDE 宿主路径。
- 禁止把 TRAE provider 名称本身当作 TRAE IDE 已完整支持的证据。
- 禁止把 RFC 目标 Endpoint 当作 Memory OS 已实现 Endpoint。
- 禁止为了测试通过而降低 fail-closed、安全、授权、Grant、状态机、Evidence 或 Goal Judge 约束。

## 8. 验收标准

- 每个核心判断都能回溯到源码和实际测试。
- 已知 2 个合同测试失败得到语义正确的最小修复，且完整门禁重新通过；若不能修复，明确阻塞。
- 自主闭环、技能进化、IDE 兼容、Memory OS 四个主题均有“设计/代码/接入/mock/真实 E2E”分层结论。
- 补丁不破坏现有架构边界，不引入凭据，不扩大外部权限，不污染锁文件。
- 对无法在单仓完成的工作给出明确对端依赖和可执行的双仓验收计划，而不是宣称完成。
- 最终输出足以让 Codex 在隔离工作树独立应用、审查和复验。
