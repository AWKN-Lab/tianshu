# 授权确认书：Phase 5 接力实查与 Phase 6 启动决策

> 文档编号：AUTH-2026-07-28-PHASE5-6
> 日期：2026-07-28
> 前置文档：21-Agent-OS-3.0总开发计划.md、2026-07-28-授权确认书-Phase4-R2ShadowIntegration前置审计与分阶段方案.md
> 状态：DRAFT_FOR_APPROVAL

## 一、环境前置审计结果（实查非摘要）

### 1.1 Git 状态

- 当前分支：`feat/r2-shadow-integration`（HEAD: `00d4082`）
- main 分支：`ead634a`（docs commit）
- 远程：`tianshu` → `git@tianshu:AWKN-Lab/tianshu.git`（无 origin）
- 本地领先 main：23 个 commit（Phase 1-4 全部工作）
- Worktree 状态：仅 `.gitignore` 修改（已清理 8 个未跟踪文件）

### 1.2 GitHub 实查状态（通过 MCP API 确认）

| 项 | GitHub 状态 | 本地状态 | 差距 |
|---|---|---|---|
| PR #64 | `open` + Draft，`merged_at: null` | 已本地合并到 feat/r2-shadow-integration | **未推送、未合并到 main** |
| Issue #66 | `open`，0 评论，所有验收项未勾选 | Phase 4 建成 shadow 基础设施 | **真实 shadow 执行未开始** |
| Issue #43 | 无 R2 Exit 决策 | 生成自测 R2 Exit Report | **自测 ≠ 真实证据** |

### 1.3 测试状态

| 测试套件 | 数量 | 通过 | 失败 | 说明 |
|---|---|---|---|---|
| Unit + Contract (`test:all`) | 384 | 384 | 0 | ✅ 全通过 |
| Verify Tests (`test:verify`) | 23 | 14 | 9 | ⚠️ 9 个文件失败 |
| Architecture Scan | - | ✅ | 0 blocking | 无新增阻断 |

### 1.4 Verify 测试失败分类

| 文件 | 失败原因 | 类别 | 修复策略 |
|---|---|---|---|
| verify-cron-inflight.ts | ~~检查旧 inFlight Set 模式~~ | ✅ 已修复 | 更新为检查 WorkStore 幂等模式 |
| verify-evolve-loop.ts (静态) | ~~recordFailure vs recordLoopFailure 命名~~ | ✅ 已修复 | 重命名 7 处调用 |
| verify-tools-error-propagation.ts (1/4) | ~~catch 块缺 recordLoopFailure~~ | ✅ 已修复 | 同上重命名 |
| verify-evolve-loop.ts (行为) | evolve 管道行为测试失败 | 真实 bug | **Phase 7 Evolve v2 修复** |
| verify-evolve-full-loop.ts | FOREIGN KEY constraint failed | 真实 bug | **Phase 7 Evolve v2 修复** |
| verify-budget-order.ts | tianhuo-cicd-loop 缺 recordCycle | 需调查 | 检查是否需要添加 |
| verify-llm-skills.ts | Cannot find module skills/parser.js | 需调查 | 检查模块是否应存在 |
| verify-loop-checkpoint.ts | LLM 失败 return 前缺 clearCheckpoint | 需调查 | 检查 agent-loop LLM 失败路径 |
| verify-cron-evolve.ts | 3 项失败 | 需调查 | 检查 cron+evolve 集成 |
| verify-cron-stop-no-zombie.ts | 检查旧 setTimeout 守卫模式 | 可能过时 | 更新为当前模式 |
| verify-react-state-purity.ts | 不明 | 需调查 | 检查 React 状态纯度 |
| verify-tools-error-propagation.ts (3/4) | 2 个注释检查 + 1 个 enriched error | 过时+需调查 | 删除注释检查，检查 enriched |

### 1.5 R2 Shadow Integration 真实状态

- **基础设施**：已建成（Feature Flag、Composition Root、4 个 Legacy Adapters、ShadowDiffReceipt、ShadowExecution、R2 Exit Report 生成器）
- **真实执行**：**未开始**。`generate-r2-exit-report.ts` 第 28 行明确标注"构造 self-test records"
- **R2 Exit Report 的 "GO" 决策**：基于 14 个合成测试记录，非真实 WP02-05 shadow 运行
- **Issue #66 验收项**：全部未勾选（0/7）

---

## 二、需要授权的决策

### 决策 A：推送本地 commit 到 GitHub

**现状**：本地 `feat/r2-shadow-integration` 分支领先远程 23 个 commit（Phase 1-4 全部工作），未推送。

**推荐**：推送 `feat/r2-shadow-integration` 到 `tianshu/feat/r2-shadow-integration`。

**风险**：推送本身不修改 main，不触发 CI（非 main 分支）。

**授权范围**：`git push tianshu feat/r2-shadow-integration`

---

### 决策 B：PR #64 合并到 main

**现状**：
- GitHub：Draft 状态，3 平台 CI 通过（run #128），276 tests 0 fail
- 3 项语义修复已完成（commit `a849d93`）：
  1. Unicode Code Point Comparator 替换 `localeCompare()`
  2. `verifyImmutableRender()` 读取时完整性验证
  3. `assertCrossFieldInvariants()` 跨字段不变量
- 本地已合并到 feat/r2-shadow-integration，与 Phase 1-3 共存验证通过（200 tests）
- **尚未完成**：人工架构与语义 Review

**推荐**：**先推送再标记 Ready for Review，不自动合并**。

**理由**：
- PR #64 的 3 项语义修复是确定性的、可测试的
- 276 个测试覆盖了所有边界情况
- 但"正式审核"需要人工架构判断，不应由执行者自行完成
- 标记 Ready for Review 后，审核可以在合并前完成

**替代方案**：如果用户认为自测足够，可以授权直接 Squash Merge。

**授权范围**：
- 选项 B1（推荐）：推送 + 标记 Ready for Review，等待审核
- 选项 B2：推送 + 直接 Squash Merge 到 main（跳过审核）
- 选项 B3：不推送，保持 Draft

---

### 决策 C：Verify 测试修复策略

**现状**：9 个 verify 测试文件失败（已修复 2 个，从 10 降至 9）。

**推荐**：
1. **立即修复**（过时测试）：更新 verify-cron-stop-no-zombie.ts、verify-tools-error-propagation.ts 的过时注释检查
2. **调查后修复**：verify-budget-order.ts、verify-llm-skills.ts、verify-loop-checkpoint.ts、verify-react-state-purity.ts
3. **延后到 Phase 7**：verify-evolve-loop.ts（行为）、verify-evolve-full-loop.ts（FK 约束）、verify-cron-evolve.ts — 这些是 evolve 管道深层问题，应在 Phase 7 (Evolve v2) 中系统性修复
4. **门禁策略**：将 `test:verify` 加入 `npm run check`，但创建 `verify-known-failures.json` 白名单。新失败会阻断 CI，已知失败不阻断

**授权范围**：
- 选项 C1（推荐）：执行 1+2+4，延后 3 到 Phase 7
- 选项 C2：全部立即修复（可能需要数小时）
- 选项 C3：保持现状，不加入 check 门禁

---

### 决策 D：R2 Shadow Integration 执行方式

**现状**：
- Shadow 基础设施已建成（Phase 4a-4f）
- 但从未用真实 WP02-05 数据运行过
- 自测 R2 Exit Report 不构成 Issue #66 验收证据

**推荐**：
1. **推送后**（决策 A），在 `feat/r2-shadow-integration` 分支上运行真实 shadow
2. **输入数据**：使用现有 contract test fixtures 作为 Engine v2 Input Snapshot
3. **执行路径**：调用 `runShadowExecution()` 传入真实 fixture 数据
4. **生成真实 R2 Exit Report**：用真实 shadow records 替换自测数据
5. **更新 Issue #66**：在 GitHub 上评论真实执行结果

**前提依赖**：
- 决策 A（推送）必须先完成
- PR #64 不需要先合并（本地已合并即可运行）

**授权范围**：
- 选项 D1（推荐）：运行真实 shadow + 生成真实报告 + 更新 Issue #66
- 选项 D2：仅运行 shadow，不更新 GitHub
- 选项 D3：跳过，使用自测报告作为临时证据

---

### 决策 E：Phase 6 启动范围

**现状**：
- Phase 6 = Policy/Skill Compiler (C04) + Tool/Model Broker (C05) + Evidence-Gain Loop (C06)
- 设计文档已冻结：05-Policy-Skill-Compiler.md、06-Tool-Model-Broker.md、07-Evidence-Gain-Loop.md
- 对应 Issue #67-#71 均为 `DESIGNED` 状态

**用户约束**："R2 通过后再启动 Policy/Skill Compiler"

**分析**：
- R2 Exit Report（自测）显示 GO，但不是真实证据
- 真实 R2 Shadow（决策 D）需要时间执行
- Phase 6 的 C04 (Policy Compiler) 依赖 WP01 (Contracts) 和 WP02 (Input)，不直接依赖 Shadow 结果
- Phase 6 的 C05 (Broker) 依赖 WP03 (Intent/Goal) 和 WP05 (Context)
- Phase 6 的 C06 (Evidence-Gain Loop) 依赖 WP03+WP05-09

**推荐**：
1. **先执行决策 D**（真实 R2 Shadow）
2. **如果真实 shadow 通过**（P0/P1 Diff = 0）：启动 Phase 6 全部三个组件
3. **如果真实 shadow 发现问题**：先修复 R2 问题，再启动 Phase 6
4. **并行准备**：可以开始 Phase 6 的代码骨架（Registry 接口、Schema 定义），但不接入主链

**授权范围**：
- 选项 E1（推荐）：先执行 D，根据结果决定是否启动 E
- 选项 E2：并行执行 D + 启动 Phase 6 代码骨架
- 选项 E3：跳过 D，直接启动 Phase 6（接受自测报告）

---

## 三、执行顺序推荐

```
1. [A] 推送 feat/r2-shadow-integration 到 GitHub
2. [B1] PR #64 标记 Ready for Review
3. [C1] 修复过时 verify 测试 + 加入 check 门禁
4. [D1] 运行真实 R2 Shadow Integration
5. 根据 D 结果：
   - GO → [E1] 启动 Phase 6
   - NO-GO → 修复 R2 问题后再启动 Phase 6
```

---

## 四、安全约束

- 不自动合并 PR 到 main（除非用户明确授权 B2）
- 不跳过真实 shadow 验证（除非用户明确授权 D3）
- 不在 verify 测试未通过时声称"门禁统一完成"
- evolve 管道 bug 不在 Phase 6 中修复（属于 Phase 7 范围）
- 所有推送操作只推送 feature 分支，不直接推送 main

---

## 五、待确认清单

请用户对以下 5 项决策逐一确认：

- [ ] **A**: 推送 feat/r2-shadow-integration 到 GitHub
- [ ] **B**: PR #64 处理方式（B1/B2/B3）
- [ ] **C**: Verify 测试修复策略（C1/C2/C3）
- [ ] **D**: R2 Shadow Integration 执行方式（D1/D2/D3）
- [ ] **E**: Phase 6 启动范围（E1/E2/E3）

默认推荐组合：**A + B1 + C1 + D1 + E1**（最安全路径）
